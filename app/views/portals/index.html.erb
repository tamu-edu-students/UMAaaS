<div class="index-background">
    <%= form_tag program_redirect_path(), :method => :get, :id => "index-search-box-form" do %>
     <div id="index-search-box" style=""><select name="program_id"><option value=0>Where to?</option>
                <%= @programs.each do |program| %>
                    <option value="<%= program.id %>"><%= program.name %></option>
                <% end %>
            </select></div>
    <%end%>
</div>